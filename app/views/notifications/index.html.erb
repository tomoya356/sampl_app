<div class="row">
  <div class="col-md-12">
    <div class="heading">
      <h1>通 知</h1>
    </div>
    <%= link_to "通知削除", notifications_path(@notifications), method: :delete ,class: "fas fa-trash" %>
    <% if @notifications.exists? %>
    　<div class="notification-index">
        <%= render @notifications %>
    　</div>
　　<% else %>
   　　 <p>通知はありません</p>
　　<% end %>
  </div>
</div>